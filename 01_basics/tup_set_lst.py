# collection = single "variable" used to convert to store multiple values
# List = [] ordered and changeable, accepts duplicates
# Set = {} unordered and immutable, allow Add/Remove but not duplicates
# Tuple = () ordered and unchangeable, allow duplicates and is faster 
fruits=["apple","banana","orange","coconut"]
print(fruits)
for fruits in fruits:
    print(fruits)
 
#  print(dir(fruits))
print(help(fruits))